<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="com.arctouch.test.vm.Resource.Status" />

        <variable
            name="resource"
            type="com.arctouch.test.vm.MoviesResource" />

        <variable
            name="query"
            type="com.arctouch.test.vm.QueryViewModel" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@android:color/black"
        android:fitsSystemWindows="true"
        android:orientation="vertical">

        <android.support.v7.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="@color/colorPrimary"
            android:minHeight="?attr/actionBarSize"
            android:theme="@style/ToolbarTheme"
            app:title="">

            <android.support.v7.widget.AppCompatEditText
                android:id="@+id/search_text"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginEnd="16dp"
                android:layout_marginRight="16dp"
                android:hint="@string/search_movie_title"
                android:text="@={query.query}" />

        </android.support.v7.widget.Toolbar>

        <RelativeLayout
            android:id="@+id/content"
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <android.support.v7.widget.RecyclerView
                android:id="@+id/movies_recycler"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:alpha="0"
                android:visibility="gone"
                app:layout_behavior="@string/appbar_scrolling_view_behavior"
                app:showIf="@{!resource.isEmpty &amp;&amp; resource.status != Status.LOADING}" />

            <ProgressBar
                android:id="@+id/progress"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_centerInParent="true"
                android:layout_gravity="center"
                android:alpha="0"
                android:visibility="gone"
                app:showIf="@{resource.status == Status.LOADING &amp;&amp; resource.isEmpty}" />


            <TextView
                android:id="@+id/error_text"
                style="@style/Text.Display"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_centerInParent="true"
                android:layout_gravity="center"
                android:layout_margin="56dp"
                android:gravity="center"
                android:text="@string/search_movie_content"
                app:resourceText="@{resource}"
                app:showIf="@{resource == null || ((resource.isEmpty || resource.status == Status.ERROR) &amp;&amp; resource.status != Status.LOADING)}" />
        </RelativeLayout>
    </LinearLayout>
</layout>